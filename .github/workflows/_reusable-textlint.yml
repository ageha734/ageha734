name: Textlint (reusable)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  textlint:
    name: Prose Lint (Japanese)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - run: docker compose -f compose.ci.yaml run --rm textlint
